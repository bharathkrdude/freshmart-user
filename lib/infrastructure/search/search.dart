import 'package:flutter/material.dart';

class Serachrepo extends StatelessWidget {
  const Serachrepo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}